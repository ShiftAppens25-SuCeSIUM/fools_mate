import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF8A5CF6);
  static const Color card = Colors.white;
  static const Color background = Color.fromRGBO(245, 245, 245, 1);
  static const Color slogan = Color.fromRGBO(97, 97, 97, 1);
  static const Color neutral = Colors.grey;
}
